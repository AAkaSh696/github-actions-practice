name: Task 2 day 45

on:
    push:
        branches: [main]

jobs:
    build-image:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout Code
              uses: actions/checkout@v4

            - name: Build the image
              run: docker build -t my_app:latest .
