name: PR check

on:
    pull_request:
        branches: [main]

jobs: 
    pull-request:
        runs-on: ubuntu-latest

        steps:
            - name: Print PR Branch
              run: echo "PR check running for branch:"  ${{ github.head_ref }}